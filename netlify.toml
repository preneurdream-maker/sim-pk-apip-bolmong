# Netlify Configuration — SIM-PK APIP (Next.js App Router)

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
  AUTH_TRUST_HOST = "true"

[[plugins]]
  package = "@netlify/plugin-nextjs"
